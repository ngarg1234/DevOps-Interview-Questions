Apache Airflow is ETL tool.. to build the data pipeline....

Works on DAG, Directive Acyclic Graph.. Directive meaning in one directory, Acyclic means no loops and Graph means visual representation.....

You define tasks in Airflows(boxes)....

Executer will run DAGS (inside Tasks)....